generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model users {
  name     String
  surname  String
  email    String  @unique
  password String
  phone    Int
  admin    Boolean
}

model products {
  id     Int      @id @default(autoincrement())
  title  String
  price  Int
  img    String
  colors String[]
}